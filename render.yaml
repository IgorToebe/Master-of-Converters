services:
  - type: web
    name: conversor-de-audio-web
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm run start:web
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
    autoDeploy: true
